# RAG Acceptance Markdown

The rag acceptance exact token proves BM25 exact retrieval across the local
profile index. Markdown ingestion must preserve raw assets, parsed text,
chunks, citation anchors, and embedding metadata.

