# Repo Docs Fixture

Repo docs describe offline file only retrieval for air profiles. The acceptance
suite uses this checked-in source document to verify file-only RAG indexing and
offline policy behavior without a network dependency.

